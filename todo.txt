getRandom(), getLast(), getFirst()
rearrange files to help IDE to handle typings and jsdoc - main file is uncompressed + d.ts + min.js in one folder
traceLabel is needed
get by id is a strict check when id is numerical string - it is disturbs
default values as a functions, forexample to add current timestamp
add Clone func, may be cloneById 
